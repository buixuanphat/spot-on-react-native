import { SafeAreaView } from "react-native-safe-area-context";

const EventDetails = ({navigation, eventId}) =>
{
    return(
        <SafeAreaView>

        </SafeAreaView>
    );
}
export default EventDetails;